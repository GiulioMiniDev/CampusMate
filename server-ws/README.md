# Server WebSocket

Servizio realtime del progetto `CampusMate` basato su `Node.js` e libreria `ws`.

## Responsabilita

- aprire connessioni WebSocket persistenti
- inviare eventi realtime ai client
- restare separato dalla logica HTTP/JSON di PHP

## Avvio locale

```bash
npm install
npm start
```

